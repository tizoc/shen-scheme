#!/bin/sh
#|
exec /usr/bin/env gosh -I`(dirname $0)`/.. -mshen.runner $0 $@
|#

(import (gauche base) (scheme base))

((setter port-buffering) (current-output-port) :none)
((setter port-buffering) (current-input-port) :none)

(include "shen/runner.sld")
